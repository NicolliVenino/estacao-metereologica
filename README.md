# Ponderada Semana 10 - Estação Meteorológica 

## Pré-requisitos
* Python 3.10+
* Bibliotecas: `flask`, `requests`

## Como Instalar e Executar

1. **Crie um ambiente virtual e rode:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/macOS
   venv\Scripts\activate     # Windows
